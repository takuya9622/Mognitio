let total :function(int,int):int= function(price: int, count: int): int {
    price * count
};
let payable :function(int,bool):int= function(amount: int, discount: bool): int {
    branch when{(discount)=>{ amount - 25 },else=>{ amount }}
};
payable(total(75, 4), true) == 275
