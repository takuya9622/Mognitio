function total(int price, int count): int {
    price * count
}
function payable(int amount, bool discount): int {
    if (discount) { amount - 25 } else { amount }
}
payable(total(75, 4), true) == 275
