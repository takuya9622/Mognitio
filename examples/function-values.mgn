let add = function(a: int, b: int): int { a + b };
let subtract = function(a: int, b: int): int { a - b };
var count: int = 0;
var total: int = 0;
loop while (count < 4) {
    total = total + (branch when{(count < 2)=>{ add },else=>{ subtract }})(10, count);
    count = count + 1;
};
total == 36
